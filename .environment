export APP_ENV=prod
export APP_DEBUG=0

# Construire l'URL de la base de données à partir de la relation Platform.sh
export DATABASE_URL=postgresql://${DATABASE_USER}:${DATABASE_PASSWORD}@${DATABASE_HOST}:${DATABASE_PORT}/${DATABASE_NAME} 